# Trainer input fix — 2026-09-13

The direct Online menu was not clickable while the trainer overlay was active.
The C script used axis/script control IDs (172–175, 201–202) for navigation and
accept/cancel. The GTAV frontend table identifies the discrete controls as:

- up/down/left/right: 165, 164, 166, 167;
- accept/cancel: 177, 178.

`script/trainer.c` now uses the frontend IDs and calls `IS_PAUSE_MENU_ACTIVE` to
close its overlay before the game's own pause UI can consume controls. The native
declaration uses the swapped SC-CL argument order needed to generate the known
64-bit hash `B0034A223497FFCB`; build translated all 45 natives successfully.

Build SHA256: `d15581fa70e82c47d6d4e490caa36d1e729f1592bfbffa7f0fe7384a92f947b2`.
Fourteen framework tests pass. This is a trainer input correction; it is not an
Online implementation.

Live sequence: payload installed for PID 150 after a previous unstarted attempt;
heartbeat then advanced. CLI `menu close` set menuOpen=0. A 30-second metadata-only
TCP sample found no Switch connection to the PC's ports 80, 18080 or 443. No raw
HTTP/ROS data was collected. Trainer remains installed with overlay closed; restart
GTA to remove all RAM modifications, or use `trainer.py stop` during ordinary
gameplay for journaled cleanup.
